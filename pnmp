Con On this branch, Azure "deploy data source", "deploy feeder access" and "request workspace" are already restricted in the backend to Admin or a Data Content Manager (DCM) of the dataset's own BU/SU, using UserService.isDataContentManagerForDataset(JpaEntity). The frontend now needs a per-dataset boolean `azureDeployable` in the dataset detail response to decide whether to show those actions.

Make exactly these changes. No refactoring, no new comments, no formatting changes to untouched lines.

1. src/main/java/com/socgen/bigdata/catalog/models/dto/DatasetWithoutDatasourceDto.java
   Directly after the `editable` field, add:
       @Schema(example = "true")
       private boolean azureDeployable;

2. src/main/java/com/socgen/bigdata/catalog/services/EntityService.java
   In the private method setRightsToDto(Long id, EntityDetailDto entityDetailDto), inside the existing `if (entity.isPresent())` block, add as the first statement:
       entityDetailDto.setAzureDeployable(
           userService.isAdmin() || userService.isDataContentManagerForDataset(entity.get())
       );
   Reuse the existing `entity` Optional. Do not add another repository call.

3. src/main/java/com/socgen/bigdata/catalog/services/FeederAccessService.java
   In createFeederAccessAndCallSelfService, replace the string literal "azure" with HostingPlatform.AZURE.name() (still using equalsIgnoreCase). Add the import for com.socgen.bigdata.catalog.enums.HostingPlatform if missing.

4. The message "Access Denied: You are not authorized as the Data Content Manager for this BU/SU." is duplicated in DataSourceService (twice) and FeederAccessService (once). Add a public static final String constant ACCESS_DENIED_DATA_CONTENT_MANAGER to UserService, and use it in all three places.

5. In DataSourceService.deployPermission, move the authorization block (the `if (!userService.isAdmin() && !userService.isDataContentManagerForDataset(dataset))` throw) and the `datasetService.getDataset(...)` call it depends on, so they run immediately after `DataSource dataSource = getDataSource(id);` and before the legacy/hosting platform/storage space/status validations. Remove the now-duplicate later getDataset call and keep using the same `dataset` variable further down.

6. Tests
   - src/test/java/com/socgen/bigdata/catalog/services/datasource/DataSourceServiceTest.java: ensure there is a `@Mock UserService userService`. In the existing Azure deploy and deployPermission success/error tests, stub `when(userService.isAdmin()).thenReturn(true)` so they pass again.
   - In DataSourceServiceTest, add tests (Given_When_Then naming like the existing ones):
     a) deploy on Azure, user is DCM of the dataset BU/SU (isAdmin false, isDataContentManagerForDataset true) -> proceeds.
     b) deploy on Azure, user not admin and not DCM for dataset -> throws InsufficientRightException with ACCESS_DENIED_DATA_CONTENT_MANAGER.
     c) deployPermission, user not admin and not DCM for dataset -> throws InsufficientRightException.
   - In the FeederAccessService test class, add: hostingPlatform "azure", user not admin and not DCM -> throws InsufficientRightException; hostingPlatform "lucid" -> no rights check.
   - In the UserService test class, add tests for isDataContentManagerForDataset: null dataset -> false; dataset with null buSuOwnership -> false; DCM with matching rights -> true.
   - In the EntityService test class, if a test covers the dataset detail path that sets `editable`, assert azureDeployable is true when isAdmin() is true and false when isAdmin() and isDataContentManagerForDataset() are both false.

After editing, run `mvn -q test` for the affected test classes, fix only failures caused by these changes, and show me the final diff for every file.
